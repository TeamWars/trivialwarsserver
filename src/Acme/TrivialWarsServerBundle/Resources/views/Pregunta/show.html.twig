{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pregunta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pregunta</th>
                <td>{{ entity.pregunta }}</td>
            </tr>
            <tr>
                <th>Idrespuestacorrecta</th>
                <td>{{ entity.idrespuestacorrecta }}</td>
            </tr>
            <tr>
                <th>Idrespuestauno</th>
                <td>{{ entity.idrespuestauno }}</td>
            </tr>
            <tr>
                <th>Idrespuestados</th>
                <td>{{ entity.idrespuestados }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pregunta') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
